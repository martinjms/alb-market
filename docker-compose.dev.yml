version: '3.8'

services:
  # Neo4j Database
  neo4j:
    image: neo4j:5.22.0-community
    container_name: alb-market-neo4j
    ports:
      - "7474:7474"  # Neo4j Browser
      - "7687:7687"  # Bolt protocol
    environment:
      - NEO4J_AUTH=neo4j/albmarket123
      - NEO4J_PLUGINS=["apoc", "graph-data-science"]
      - NEO4J_dbms_memory_pagecache_size=1G
      - NEO4J_dbms_memory_heap_initial__size=1G
      - NEO4J_dbms_memory_heap_max__size=2G
      - NEO4J_apoc_export_file_enabled=true
      - NEO4J_apoc_import_file_enabled=true
      - NEO4J_apoc_import_file_use__neo4j__config=true
    volumes:
      - neo4j-data:/data
      - neo4j-logs:/logs
      - neo4j-import:/var/lib/neo4j/import
      - neo4j-plugins:/plugins
      - ./packages/database/backups:/backups
    healthcheck:
      test: ["CMD", "neo4j", "status"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - alb-market-network

  # Backend API (will be added when backend is ready)
  # backend:
  #   build:
  #     context: ./packages/backend
  #     dockerfile: Dockerfile.dev
  #   container_name: alb-market-backend
  #   ports:
  #     - "4000:4000"
  #   environment:
  #     - NODE_ENV=development
  #     - NEO4J_URI=bolt://neo4j:7687
  #     - NEO4J_USER=neo4j
  #     - NEO4J_PASSWORD=albmarket123
  #     - PORT=4000
  #   volumes:
  #     - ./packages/backend:/app
  #     - /app/node_modules
  #   depends_on:
  #     neo4j:
  #       condition: service_healthy
  #   networks:
  #     - alb-market-network

  # Frontend (will be added when frontend is ready)
  # frontend:
  #   build:
  #     context: ./packages/frontend
  #     dockerfile: Dockerfile.dev
  #   container_name: alb-market-frontend
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=development
  #     - VITE_API_URL=http://localhost:4000
  #   volumes:
  #     - ./packages/frontend:/app
  #     - /app/node_modules
  #   networks:
  #     - alb-market-network

volumes:
  neo4j-data:
  neo4j-logs:
  neo4j-import:
  neo4j-plugins:

networks:
  alb-market-network:
    driver: bridge